import { ScrollFxDirective } from './scroll-fx.directive';

describe('ScrollFxDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollFxDirective();
    expect(directive).toBeTruthy();
  });
});
