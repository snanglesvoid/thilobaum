import { ImagesLoadedDirective } from './images-loaded.directive';

describe('ImagesLoadedDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagesLoadedDirective();
    expect(directive).toBeTruthy();
  });
});
